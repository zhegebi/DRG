[project]
name = "drg-server"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = ["fastapi[standard]>=0.136.0"]

[dependency-groups]
dev = [
    "concurrently>=2.1",
    "sh>=2.2.2",
    "shx>=0.4.2",
]
