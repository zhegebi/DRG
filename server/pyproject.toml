[project]
name = "drg-server"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.136.0",
]

[project.scripts]
dev = "script.runner:dev"
prod = "script.runner:prod"

[tool.setuptools]
packages = ["script"]